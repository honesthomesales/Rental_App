import { getSupabaseClient, handleSupabaseError, createApiResponse } from '../client';
import type { RentPeriod, Tenant, Lease, ApiResponse } from '../types';

export class RentPeriodsService {
  static async getAllRentPeriods() {
    return [];
  }
}
